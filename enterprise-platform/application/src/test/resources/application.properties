U# Unit test properties.
# NOTE: @WebMvcTest already excludes JPA/DataSource/Flyway auto-configuration
# by design - no explicit exclusions needed here.
# This file must NOT exclude JPA/DataSource as it would also affect
# integration tests (ItemIT) which share this classpath and need a real DB.
spring.flyway.enabled=false
