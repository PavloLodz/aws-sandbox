---
# Variables shared across all app_servers regardless of environment.
# Environment-specific overrides live in inventory/dev/ or inventory/prod/.

app_name: aws-attempt
app_user: spring
app_group: spring
app_dir: /opt/aws-attempt
app_port: 8080
java_package: java-21-amazon-corretto
