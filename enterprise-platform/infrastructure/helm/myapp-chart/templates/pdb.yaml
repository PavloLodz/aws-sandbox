apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: myapp
  namespace: myapp
  labels:
    app: myapp
    version: "0.0.1"
    component: pdb
    managed-by: kubectl
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: myapp
